# Day 3
Date: 2026-06-30

## 研究テーマ
試合映像だけから若手選手の特徴を定量化し、類似選手の提示と成長可能性の分析を通じて、指導者の育成・スカウティングを支援するコンピュータビジョンシステム

## 今日の目標
- OpenCVを用いて試合動画を読み込む
- YOLOを利用する前段階として動画処理の仕組みを理解する

## 実施内容
- `video_reader.py` を研究用コードとして整理した。
- `pathlib.Path` を用いて動画パスを管理した。
- 動画ファイルの存在確認を追加した。
- `cv2.VideoCapture()` を利用して動画を読み込んだ。
- `cap.isOpened()` を用いて動画が正常に開けているか確認した。
- 動画のFPSを取得し、再生速度を動画に合わせて調整した。
- `q` キーで動画を終了できるように実装した。
- `cap.release()` と `cv2.destroyAllWindows()` により後処理を実装した。
- OpenCVで試合動画を正常に読み込めることを確認した。

## 発生した問題


---

**学んだこと**

研究では実行環境を統一することが重要であり、仮想環境を利用することでライブラリやPythonバージョンをプロジェクトごとに管理できる。

## 今日学んだこと

- OpenCVは画像・動画を扱うためのComputer Visionライブラリである。
- `VideoCapture` を利用することで動画をFrame単位で取得できる。
- 動画は静止画(Frame)の連続で構成されている。
- `FPS(Frame Per Second)` は動画1秒間に表示されるFrame数である。
- AIを利用する前に入力データが正常に読み込めていることを確認することが重要である。
- エラーは最後のメッセージから原因を特定する習慣が重要である。

## 今日の成果


- 動画の読み込み成功
- 最初のフレーム取得成功
- FPS取得成功

## 研究者メモ




